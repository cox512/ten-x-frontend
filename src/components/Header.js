import React from "react";
import { Link } from "react-router-dom";

const Header = () => {
  return (
    <div>
      HEADER
      <Link to="/">Login</Link>
    </div>
  );
};

export default Header;
